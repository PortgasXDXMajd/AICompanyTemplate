# Product repos are cloned into projects/ but tracked in their own GitHub repos
/projects/*
!/projects/README.md

# Employee worktrees: one checkout per running employee, branches live in the repos themselves
/worktrees/*
!/worktrees/README.md

# Local-only Claude Code state
.claude/settings.local.json
.claude/agent-memory-local/

# Secrets and OS noise
.env
.env.*
!.env.example
.DS_Store
